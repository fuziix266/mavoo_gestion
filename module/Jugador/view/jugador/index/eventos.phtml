<?php $this->headTitle('Eventos'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-calendar-event me-2"></i>Eventos disponibles</h4>
                <?php if (empty($eventos)): ?>
                    <div class="alert alert-secondary text-center py-4">
                        <i class="ti ti-calendar-off fs-1 d-block mb-2"></i>No hay eventos registrados.
                    </div>
                <?php else: ?>
                <div class="row g-3">
                    <?php foreach ($eventos as $e): ?>
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="mb-1"><?= htmlspecialchars($e['titulo'], ENT_QUOTES, 'UTF-8') ?></h5>
                                <p class="mb-1"><span class="badge bg-primary-subtle text-primary text-uppercase"><?= htmlspecialchars($e['deporte'], ENT_QUOTES, 'UTF-8') ?></span></p>
                                <p class="text-muted small mb-0">
                                    <i class="ti ti-calendar"></i>
                                    <?= $e['referencia_utc'] ? htmlspecialchars(date('d M Y H:i', strtotime($e['referencia_utc'])), ENT_QUOTES, 'UTF-8') : '—' ?>
                                </p>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
