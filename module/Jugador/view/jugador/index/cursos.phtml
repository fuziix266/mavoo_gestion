<?php $this->headTitle('Cursos'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-book me-2"></i>Cursos disponibles</h4>
                <?php if (empty($cursos)): ?>
                    <div class="alert alert-secondary text-center py-4">No hay cursos próximos.</div>
                <?php else: ?>
                <div class="row g-3">
                    <?php foreach ($cursos as $c): ?>
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="mb-1"><?= htmlspecialchars($c['nombre_evento'] ?? '', ENT_QUOTES, 'UTF-8') ?></h5>
                                <span class="badge bg-primary-subtle text-primary text-uppercase"><?= htmlspecialchars($c['deporte'] ?? '', ENT_QUOTES, 'UTF-8') ?></span>
                                <p class="mt-2 mb-1"><strong>Desde:</strong> <?= htmlspecialchars($c['fecha_inicio'] ?? '-', ENT_QUOTES, 'UTF-8') ?></p>
                                <p class="mb-0"><strong>Valor:</strong> $<?= number_format((float)($c['valor_inscripcion'] ?? 0), 0, ',', '.') ?></p>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
