<?php
$this->headTitle('Deporte: ' . ucfirst($this->deporte));
$deporte = $this->deporte ?? 'padel';
?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3">
                    <i class="ti ti-trophy me-2"></i>
                    <?= htmlspecialchars(ucfirst($deporte), ENT_QUOTES, 'UTF-8') ?>
                </h4>
                <p class="text-muted">Eventos próximos y ranking de la disciplina.</p>

                <ul class="nav nav-tabs mb-3" role="tablist">
                    <li class="nav-item">
                        <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#proximos">Próximos eventos</button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#ranking">Ranking</button>
                    </li>
                </ul>

                <div class="tab-content">
                    <div class="tab-pane fade show active" id="proximos">
                        <?php if (empty($eventos)): ?>
                            <div class="alert alert-secondary text-center py-3">
                                <i class="ti ti-calendar-off me-2"></i>No hay eventos próximos de <?= htmlspecialchars($deporte, ENT_QUOTES, 'UTF-8') ?>.
                            </div>
                        <?php else: ?>
                            <ul class="list-group">
                                <?php foreach ($eventos as $e): ?>
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                        <strong><?= htmlspecialchars($e['titulo'] ?? '', ENT_QUOTES, 'UTF-8') ?></strong>
                                        <span class="badge bg-secondary-subtle text-secondary ms-2 text-uppercase"><?= htmlspecialchars($e['inscripcion'] ?? '', ENT_QUOTES, 'UTF-8') ?></span>
                                    </div>
                                    <small class="text-muted">
                                        <?= $e['referencia_utc'] ? htmlspecialchars(date('d/m/Y H:i', strtotime($e['referencia_utc'])), ENT_QUOTES, 'UTF-8') : '' ?>
                                    </small>
                                </li>
                                <?php endforeach; ?>
                            </ul>
                        <?php endif; ?>
                    </div>
                    <div class="tab-pane fade" id="ranking">
                        <p class="text-muted text-center py-3">El ranking estará disponible próximamente.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
