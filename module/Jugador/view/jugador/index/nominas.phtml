<?php $this->headTitle('Nóminas'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-list me-2"></i>Nóminas</h4>
                <?php if (empty($nominas)): ?>
                    <div class="alert alert-secondary text-center py-4">No hay nóminas registradas.</div>
                <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr><th>ID</th><th>UUID</th><th>Orden</th><th>Ranking Auto</th><th>Ranking Manual</th></tr>
                        </thead>
                        <tbody>
                        <?php foreach ($nominas as $n): ?>
                        <tr>
                            <td>#<?= (int)$n['id'] ?></td>
                            <td><code class="text-muted small"><?= htmlspecialchars($n['uuid'] ?? '', ENT_QUOTES, 'UTF-8') ?></code></td>
                            <td><?= (int)($n['orden'] ?? 0) ?></td>
                            <td><?= (int)($n['rankingt_auto'] ?? 0) ?></td>
                            <td><?= (int)($n['rankingt_manual'] ?? 0) ?></td>
                        </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
