<?php
/**
 * vista.phtml - layout base para vistas del módulo Jugador
 * Reutiliza el layout principal de la app (ya está en /mavoo-page).
 */
?>
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="mb-3"><?= htmlspecialchars($titulo ?? 'Jugador', ENT_QUOTES, 'UTF-8') ?></h4>
                    <p class="text-muted"><?= htmlspecialchars($descripcion ?? '', ENT_QUOTES, 'UTF-8') ?></p>
                    <?= $this->content ?>
                </div>
            </div>
        </div>
    </div>
</div>
