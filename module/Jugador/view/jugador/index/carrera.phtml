<?php
$this->headTitle('Carrera Deportiva - Inicio');
?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3">
                    <i class="ti ti-flag me-2"></i>Carrera Deportiva
                </h4>
                <p class="text-muted mb-4">
                    Vista general de tu carrera: eventos pasados y próximos.
                </p>

                <?php if (empty($eventos)): ?>
                    <div class="alert alert-secondary text-center py-4">
                        <i class="ti ti-calendar-off fs-1 d-block mb-2"></i>
                        Aún no tienes eventos asociados.
                        <br><small>Los eventos en los que participes aparecerán aquí.</small>
                    </div>
                <?php else: ?>
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>Evento</th>
                                    <th>Deporte</th>
                                    <th>Inicio</th>
                                    <th>Inscripción</th>
                                </tr>
                            </thead>
                            <tbody>
                            <?php foreach ($eventos as $e): ?>
                                <tr>
                                    <td><strong><?= htmlspecialchars($e['titulo'] ?? '-', ENT_QUOTES, 'UTF-8') ?></strong></td>
                                    <td>
                                        <span class="badge bg-primary-subtle text-primary text-uppercase">
                                            <?= htmlspecialchars($e['deporte'] ?? '', ENT_QUOTES, 'UTF-8') ?>
                                        </span>
                                    </td>
                                    <td><?= !empty($e['fecha_inicio']) ? htmlspecialchars(date('d M Y', strtotime($e['fecha_inicio'])), ENT_QUOTES, 'UTF-8') : '—' ?></td>
                                    <td>
                                        <span class="badge bg-<?= ($e['inscripcion'] ?? '') === 'abierta' ? 'success' : 'secondary' ?>-subtle text-<?= ($e['inscripcion'] ?? '') === 'abierta' ? 'success' : 'secondary' ?>">
                                            <?= htmlspecialchars(strtoupper($e['inscripcion'] ?? 'cerrada'), ENT_QUOTES, 'UTF-8') ?>
                                        </span>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
