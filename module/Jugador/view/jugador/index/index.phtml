<?php
$this->headTitle('Inicio - Jugador');
$user = $this->user ?? [];
?>
<div class="row g-3 mb-4">
    <div class="col-md-3 col-sm-6">
        <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="bg-primary-100 text-primary p-3 b-r-12"><i class="ti ti-trophy fs-3"></i></div>
                <div>
                    <div class="text-muted small text-uppercase fw-semibold">Eventos activos</div>
                    <div class="fs-4 fw-bold"><?= (int)($stat_eventos ?? 0) ?></div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3 col-sm-6">
        <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="bg-success-100 text-success p-3 b-r-12"><i class="ti ti-calendar fs-3"></i></div>
                <div>
                    <div class="text-muted small text-uppercase fw-semibold">Próximos partidos</div>
                    <div class="fs-4 fw-bold"><?= (int)($stat_partidos ?? 0) ?></div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3 col-sm-6">
        <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="bg-info-100 text-info p-3 b-r-12"><i class="ti ti-medal fs-3"></i></div>
                <div>
                    <div class="text-muted small text-uppercase fw-semibold">Ranking</div>
                    <div class="fs-4 fw-bold">#<?= (int)($stat_ranking ?? 0) ?></div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3 col-sm-6">
        <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="bg-warning-100 text-warning p-3 b-r-12"><i class="ti ti-star fs-3"></i></div>
                <div>
                    <div class="text-muted small text-uppercase fw-semibold">Disciplinas</div>
                    <div class="fs-4 fw-bold"><?= (int)($stat_disciplinas ?? 0) ?></div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h5 class="mb-3">Bienvenido, <?= htmlspecialchars($user['name'] ?? 'Jugador', ENT_QUOTES, 'UTF-8') ?></h5>
                <p class="text-muted">Tu portal personal de carrera deportiva, eventos y estadísticas.</p>
                <div class="alert alert-info" role="alert">
                    <i class="ti ti-info-circle me-2"></i>
                    Usa el menú superior para navegar: <strong>Carrera Deportiva</strong>, <strong>Mis Disciplinas</strong>, <strong>Entrenador</strong>.
                </div>
            </div>
        </div>
    </div>
</div>
