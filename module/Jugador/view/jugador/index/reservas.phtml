<?php $this->headTitle('Reservas'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-calendar me-2"></i>Reservas</h4>
                <?php if (empty($reservas)): ?>
                    <div class="alert alert-secondary text-center py-4">
                        <i class="ti ti-calendar-off fs-1 d-block mb-2"></i>No hay reservas registradas.
                    </div>
                <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr><th>Fecha</th><th>Hora</th><th>Estado</th><th>Total</th></tr>
                        </thead>
                        <tbody>
                        <?php foreach ($reservas as $r): ?>
                        <tr>
                            <td><?= htmlspecialchars($r['fecha'] ?? '', ENT_QUOTES, 'UTF-8') ?></td>
                            <td><?= htmlspecialchars(($r['hora_inicio'] ?? '') . ' - ' . ($r['hora_fin'] ?? ''), ENT_QUOTES, 'UTF-8') ?></td>
                            <td><span class="badge bg-info-subtle text-info"><?= htmlspecialchars($r['estado'] ?? '', ENT_QUOTES, 'UTF-8') ?></span></td>
                            <td>$<?= number_format((float)($r['total_pago'] ?? 0), 0, ',', '.') ?></td>
                        </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
