<?php $this->headTitle('Ajustes de Carrera'); $user = $this->user ?? []; ?>
<div class="row g-3">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-settings me-2"></i>Ajustes</h4>
                <p class="text-muted">Configura tu perfil de carrera y notificaciones.</p>

                <form>
                    <div class="mb-3">
                        <label class="form-label">Email de notificaciones</label>
                        <input type="email" class="form-control" value="<?= htmlspecialchars($user['email'] ?? '', ENT_QUOTES, 'UTF-8') ?>" disabled>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Nombre</label>
                        <input type="text" class="form-control" value="<?= htmlspecialchars($user['name'] ?? '', ENT_QUOTES, 'UTF-8') ?>" disabled>
                    </div>
                    <div class="form-check form-switch mb-3">
                        <input class="form-check-input" type="checkbox" id="notifEmail" checked>
                        <label class="form-check-label" for="notifEmail">Recibir notificaciones por email</label>
                    </div>
                    <div class="form-check form-switch mb-3">
                        <input class="form-check-input" type="checkbox" id="notifSms">
                        <label class="form-check-label" for="notifSms">Recibir notificaciones por SMS</label>
                    </div>
                    <button type="button" class="btn btn-primary" disabled>Guardar (próximamente)</button>
                </form>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="mb-3">Tu cuenta</h5>
                <p class="text-muted small">
                    <strong>Email:</strong> <?= htmlspecialchars($user['email'] ?? '', ENT_QUOTES, 'UTF-8') ?><br>
                    <strong>ID:</strong> #<?= (int)($user['id'] ?? 0) ?>
                </p>
            </div>
        </div>
    </div>
</div>
