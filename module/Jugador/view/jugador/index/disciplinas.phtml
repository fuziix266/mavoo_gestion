<?php $this->headTitle('Disciplinas'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3">
                    <i class="ti ti-bookmark me-2"></i>Disciplinas disponibles
                </h4>
                <p class="text-muted mb-4">Explora las disciplinas deportivas registradas en la plataforma.</p>

                <?php if (empty($deportes)): ?>
                    <div class="alert alert-warning">No hay disciplinas activas en este momento.</div>
                <?php else: ?>
                <div class="row g-3">
                    <?php foreach ($deportes as $d): ?>
                    <div class="col-md-4 col-sm-6">
                        <a href="<?= $this->url('jugador', ['action' => 'deporte', 'id' => $d['alias']]) ?>"
                           class="card h-100 text-decoration-none text-reset hover-lift">
                            <div class="card-body text-center">
                                <div class="bg-primary-subtle text-primary rounded-circle d-inline-flex align-items-center justify-content-center mb-3"
                                     style="width:60px;height:60px;">
                                    <i class="ti ti-trophy fs-2"></i>
                                </div>
                                <h5 class="mb-1"><?= htmlspecialchars($d['deporte'], ENT_QUOTES, 'UTF-8') ?></h5>
                                <small class="text-muted text-uppercase"><?= htmlspecialchars($d['alias'], ENT_QUOTES, 'UTF-8') ?></small>
                            </div>
                        </a>
                    </div>
                    <?php endforeach; ?>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
