<?php $this->headTitle('Clases'); ?>
<div class="row g-3">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <h4 class="mb-3"><i class="ti ti-school me-2"></i>Clases y horarios</h4>
                <p class="text-muted">Sedes disponibles y bloques horarios programados.</p>
                <?php if (empty($sedes)): ?>
                    <div class="alert alert-secondary text-center py-4">No hay clases programadas.</div>
                <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr><th>Sede</th><th>Inicio</th><th>Término</th><th>Estado</th></tr>
                        </thead>
                        <tbody>
                        <?php foreach ($sedes as $s): ?>
                        <tr>
                            <td><code class="text-muted"><?= htmlspecialchars(substr($s['uuid'] ?? '', 0, 8), ENT_QUOTES, 'UTF-8') ?>…</code></td>
                            <td><?= htmlspecialchars($s['fecha_inicio'] ?? '', ENT_QUOTES, 'UTF-8') ?></td>
                            <td><?= htmlspecialchars($s['fecha_cierre'] ?? '', ENT_QUOTES, 'UTF-8') ?></td>
                            <td><span class="badge bg-<?= ($s['activo'] ?? 0) ? 'success' : 'secondary' ?>-subtle text-<?= ($s['activo'] ?? 0) ? 'success' : 'secondary' ?>"><?= ($s['activo'] ?? 0) ? 'Activo' : 'Inactivo' ?></span></td>
                        </tr>
                        <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
