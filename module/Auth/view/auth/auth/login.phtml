<?php
/**
 * Vista: Login
 * Equivalente a resources/views/auth/login.blade.php de Laravel
 * Mantiene el diseño original (gradiente azul-cyan, card glassmorphism).
 */
$this->headTitle('Iniciar Sesión | Mavoo Gestión');

// Base path para assets
$base = $this->basePath();
?>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión | Mavoo Gestión</title>
    <meta name="description" content="Plataforma de Gestión Mavoo - Acceso al sistema">

    <!-- Bootstrap CSS desde assets compartidos con Laravel -->
    <link rel="stylesheet" href="<?= $base ?>/assets/vendor/bootstrap/bootstrap.min.css">
    <!-- Phosphor Icons -->
    <link href="<?= $base ?>/assets/vendor/phosphor/phosphor-bold.css" rel="stylesheet">

    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }

        body.sign-in-bg {
            background: linear-gradient(135deg, #0000ff 0%, #00d3d3 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            position: relative;
            overflow: hidden;
        }

        body.sign-in-bg::before {
            content: '';
            position: absolute;
            width: 400px;
            height: 400px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            top: -100px;
            right: -100px;
            animation: float 6s ease-in-out infinite;
        }

        body.sign-in-bg::after {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 50%;
            bottom: -80px;
            left: -80px;
            animation: float 8s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50%       { transform: translateY(20px); }
        }

        .login-card {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            border-radius: 24px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
            padding: 50px 45px;
            max-width: 480px;
            width: 100%;
            margin: 20px;
            position: relative;
            z-index: 1;
            border: 1px solid rgba(255, 255, 255, 0.5);
        }

        .logo-container {
            text-align: center;
            margin-bottom: 35px;
            position: relative;
        }

        .logo-container img {
            max-width: 180px;
            height: auto;
            filter: drop-shadow(0 4px 12px rgba(0, 0, 255, 0.15));
        }

        .logo-container::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 4px;
            background: linear-gradient(90deg, #0000ff, #00d3d3);
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }

        .welcome-text h2 {
            color: #212529;
            font-weight: 700;
            font-size: 30px;
            margin-bottom: 8px;
            text-align: center;
            background: linear-gradient(135deg, #0000ff, #00d3d3);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .welcome-text p {
            color: #495057;
            font-size: 15px;
            text-align: center;
            margin-bottom: 35px;
        }

        .form-floating > .form-control {
            border: 2px solid #e9ecef;
            border-radius: 14px;
            padding: 12px 20px;
            height: 58px;
            transition: all 0.3s ease;
            background: #f8f9fa;
        }

        .form-floating > .form-control:focus {
            border-color: #0000ff;
            box-shadow: 0 0 0 4px rgba(0, 0, 255, 0.12);
            background: white;
        }

        .form-floating > label {
            color: #6c757d;
            padding: 16px 20px;
        }

        .btn-login {
            background: linear-gradient(135deg, #0000ff 0%, #00d3d3 100%);
            border: none;
            border-radius: 14px;
            padding: 16px;
            font-weight: 700;
            font-size: 16px;
            letter-spacing: 0.5px;
            transition: all 0.4s ease;
            box-shadow: 0 8px 24px rgba(0, 0, 255, 0.25);
            position: relative;
            overflow: hidden;
            width: 100%;
            color: white;
            cursor: pointer;
        }

        .btn-login::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.5s;
        }

        .btn-login:hover::before { left: 100%; }

        .btn-login:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 32px rgba(0, 0, 255, 0.35);
        }

        .btn-login:active { transform: translateY(-1px); }

        .form-check-input {
            width: 20px;
            height: 20px;
            border: 2px solid #dee2e6;
            border-radius: 6px;
        }

        .form-check-input:checked {
            background-color: #0000ff;
            border-color: #0000ff;
        }

        .form-check-label {
            color: #495057;
            font-size: 14px;
            font-weight: 500;
            margin-left: 8px;
        }

        .error-alert {
            background: #fff5f5;
            border: 1px solid #f5c6cb;
            color: #721c24;
            border-radius: 10px;
            padding: 12px 16px;
            margin-bottom: 20px;
            font-size: 14px;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .mb-3 { margin-bottom: 1rem; }
        .mb-4 { margin-bottom: 1.5rem; }
        .text-end { text-align: right; }
        .mt-2 { margin-top: 0.5rem; }

        a.forgot-link {
            color: #0000ff;
            text-decoration: none;
            font-weight: 600;
            font-size: 13px;
            transition: color 0.3s ease;
        }
        a.forgot-link:hover { color: #00d3d3; }
    </style>
</head>

<body class="sign-in-bg">
    <div style="width: 530px; max-width: 100%; padding: 0 15px;">
        <div class="login-card">
            <!-- Logo -->
            <div class="logo-container">
                <img alt="Mavoo Logo" src="<?= $base ?>/mavoo.png">
            </div>

            <!-- Bienvenida -->
            <div class="welcome-text">
                <h2>¡Bienvenido!</h2>
                <p>Inicia sesión para acceder al sistema de gestión</p>
            </div>

            <!-- Error de autenticación -->
            <?php if (!empty($error)): ?>
                <div class="error-alert">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
                    </svg>
                    <?= $this->escapeHtml($error) ?>
                </div>
            <?php endif; ?>

            <!-- Formulario -->
            <form method="POST" action="<?= $this->url('auth.login') ?>">
                <div class="mb-3">
                    <div class="form-floating">
                        <input
                            id="email"
                            type="email"
                            name="email"
                            class="form-control"
                            placeholder="Email"
                            value="<?= $this->escapeHtmlAttr($_POST['email'] ?? '') ?>"
                            autocomplete="username"
                            required
                            autofocus
                        >
                        <label for="email">Correo electrónico</label>
                    </div>
                </div>

                <div class="mb-3">
                    <div class="form-floating">
                        <input
                            id="password"
                            type="password"
                            name="password"
                            class="form-control"
                            placeholder="Password"
                            autocomplete="current-password"
                            required
                        >
                        <label for="password">Contraseña</label>
                    </div>
                </div>

                <div class="mb-4">
                    <div class="d-flex align-items-center" style="display:flex; align-items:center; gap:8px;">
                        <input class="form-check-input" type="checkbox" name="remember" id="remember">
                        <label class="form-check-label" for="remember">Recordarme</label>
                    </div>
                </div>

                <div class="mb-3">
                    <button type="submit" class="btn-login">
                        Iniciar Sesión
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="<?= $base ?>/assets/vendor/bootstrap/bootstrap.bundle.min.js"></script>
</body>
</html>
